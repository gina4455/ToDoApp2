{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "birthdate": {
      "description": "The birthdate of the user as a Firestore timestamp.",
      "type": "string",
      "format": "date-time"
    },
    "created_time": {
      "description": "The timestamp when the user was created.",
      "type": "string",
      "format": "date-time"
    },
    "display_name": {
      "description": "The display name of the user.",
      "type": "string"
    },
    "email": {
      "description": "The email address of the user.",
      "type": "string",
      "format": "email"
    },
    "photo_url": {
      "description": "The URL for the user's profile photo.",
      "type": "string"
    },
    "uid": {
      "description": "The unique identifier for the user.",
      "type": "string"
    }
  },
  "required": ["birthdate", "created_time", "display_name", "email", "uid"]
}
